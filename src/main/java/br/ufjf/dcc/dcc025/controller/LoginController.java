package br.ufjf.dcc.dcc025.controller;

import br.ufjf.dcc.dcc025.view.LoginView;
import br.ufjf.dcc.dcc025.model.Usuario;

/* 
// filtra o tipo de usuário e abre a tela correspondente
if (usuario instanceof Medico medico) {
    abrirMedico(medico);
}
else if (usuario instanceof Recepcionista recepcionista) {
    abrirRecepcionista(recepcionista);
}
else if (usuario instanceof Paciente paciente) {
    abrirPaciente(paciente);
}


//abre as telas de cada usuário
private void abrirMedico(Medico medico) {
    MedicoView medicoView = new MedicoView();
    new MedicoController(medico, medicoView);

    medicoView.setVisible(true);
    view.dispose();
}

private void abrirRecepcionista(Recepcionista recepcionista) {
    RecepcionistaView recepView = new RecepcionistaView();
    new RecepcionistaController(recepcionista, recepView);

    recepView.setVisible(true);
    view.dispose();
}

private void abrirPaciente(Paciente paciente) {
    PacienteView pacienteView = new PacienteView();
    new PacienteController(paciente, pacienteView);

    pacienteView.setVisible(true);
    view.dispose();
}
    */






